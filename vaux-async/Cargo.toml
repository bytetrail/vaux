[package]
name = "vaux-async"
version.workspace = true
edition.workspace = true

[dependencies]
tokio = { version = "1.41.0", features = ["io-util", "net", "time", "macros"] }
vaux-mqtt = { path = "../vaux-mqtt" }
bytes = "1.8"
tokio-rustls = { version = "0.26" }
